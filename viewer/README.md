# viewer

A minimal viewer, CLI or small web page, that pulls and renders the audit trail produced by the spine. Its job is to make the single end-to-end audit trail (inbound gate plus outbound defense) legible for the demo and for judges.

Nothing is built yet, this run only creates the folder.
