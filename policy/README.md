# policy

The declarative policy engine and policy definitions. Every action that reaches the spine is evaluated here before it is allowed to be signed. Covers both what the agent is allowed to do on its own initiative and what the outbound defense flows are allowed to trigger.

Nothing is built yet, this run only creates the folder.
